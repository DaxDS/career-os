/**
 * @deprecated Use `npm run seed:demo-data` instead (full profile, matches, pathway report).
 *
 * Usage (from repo root):
 *   NEXT_PUBLIC_SUPABASE_URL=... SUPABASE_SERVICE_ROLE_KEY=... npm run seed:demo-data
 *
 * Or with SUPABASE_ACCESS_TOKEN (fetches service role automatically):
 *   SUPABASE_ACCESS_TOKEN=sbp_... NEXT_PUBLIC_SUPABASE_URL=... npm run seed:demo-data
 */
import "./seed-demo-data.mjs";
